/** Scroll position below which the header always stays visible. */
export const MENU_HEADER_SCROLL_TOP_VISIBLE_PX = 16;

/** Minimum scroll delta before toggling header visibility. */
export const MENU_HEADER_SCROLL_DIRECTION_DELTA_PX = 5;

/** Header show/hide animation duration (ms). */
export const MENU_HEADER_TRANSITION_MS = 300;
